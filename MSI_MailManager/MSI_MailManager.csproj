<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netcoreapp3.0</TargetFramework>
    <PackOnBuild>true</PackOnBuild>
    <PackageVersion>0.5</PackageVersion>
    <Authors>Microsource </Authors>
    <Description>Implemented the following changes:
1. Allows the client to insert attachments to e-mail
2. Gives an option to compress files before attaching it to the e-mail
3. Removed the "ToName" property of the Email.RecipinetInformation class
4. Allws the client to send e-mail to multiple recipients
5. Added unit test cases for the recent changes</Description>
    <PackageId>MSI_MailManager</PackageId>
    <Copyright>Microsource</Copyright>
    <Owners>Microsource</Owners>
    <PackageReleaseNotes>Implemented the following changes:
1. Allows the client to insert attachments to e-mail
2. Gives an option to compress files before attaching it to the e-mail
3. Removed the "ToName" property of the Email.RecipinetInformation class
4. Allws the client to send e-mail to multiple recipients
5. Added unit test cases for the recent changes

Sample code can be found here:
https://dotnetfiddle.net/XrDRCT</PackageReleaseNotes>
    <Summary>MSI_MailManager</Summary>
    <PackageTags>MSI_MailManager</PackageTags>
    <Title>MSI_MailManager</Title>
    <PackageProjectUrl>https://github.com/MicroSourcePH/MailManager</PackageProjectUrl>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="NuGet.Build.Packaging" Version="0.2.5-dev.8">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
  </ItemGroup>
  <ItemGroup>
    <Folder Include="Models\" />
  </ItemGroup>
</Project>
